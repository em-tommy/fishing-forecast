# 引き継ぎメモ（開発経緯）

このファイルは**次にこのリポジトリを触る人（未来の自分・別のAIエージェント）向け**の記録。
「何ができるか」は [README.md](README.md)、「どう使うか」はアプリ内の **使い方タブ** にある。
ここに書くのは **なぜそうなっているか** と **何を試して捨てたか** だけ。

- 公開先: https://em-tommy.github.io/fishing-forecast/
- リポジトリ: https://github.com/em-tommy/fishing-forecast
- 初回コミット 2026-07-28、この記録の時点 2026-07-29

---

## 1. 何のために作ったか

利用者は品川在住・57歳、1級小型船舶免許あり。釣行先は2系統。

| | 場所 | 釣り方 | 狙い | 頻度 |
|---|---|---|---|---|
| ボート | 直江津 第三堤防沖（新潟県上越市） | 友人と共有の**2馬力・3m ボート**を直江津の海岸から出す | 尺アジ・マダイ・青物 | 月1回程度 |
| 陸っぱり | 東扇島西公園 / ふれーゆ裏 / 若洲海浜公園 / 大黒ふ頭 / 本牧 | 東京湾の岸壁 | アジ・タコ | 随時 |

**置き換えたかった作業**: 出船日を決めるのに Windy で複数の気象モデルを手で見比べ、
風速と波高から一番穏やかな日を目視で探していた。毎回15〜30分かかり、
判断基準が頭の中にしかないので再現性がなかった。

なので本アプリの主目的は「天気を表示すること」ではなく、
**根拠データつきで候補日を自動で順位づけすること**。ここを見失うと、
ただの天気サイトになって価値が消える。

### 2馬力3mボートという前提が効いている箇所

- しきい値が一般的な釣り船よりかなり厳しい（◎が風4.0m/s以下・波0.30m以下）
- **長周期うねりで1段階格下げ**する（波高の数字が小さくても3m艇は揺れる）
- **雷は風波が穏やかでも即 ×**（逃げ場がない）
- **吹き付け風で格下げ**（帰航できなくなる向き）。直江津の海岸は北〜北北東向き

---

## 2. 利用者が決めたこと（勝手に変えない）

AskUserQuestion で明示的に選ばれた設計。

| 論点 | 決定 |
|---|---|
| 公開方法 | **GitHub Pages の静的サイト**（Vultr VPS も持っているが、こちらを選択） |
| しきい値 | **画面から調整できる**（コードに固定しない） |
| モデル比較の見せ方 | **一致度サマリを既定**、詳細は展開して見る |
| 候補日の優先 | **連続する凪日**を優先 |
| 判定時間帯 | 当初は朝マヅメのみ → 後に **朝マヅメ / 昼間 / 夕マヅメ / 終日** の選択式に拡張 |

その後の指示で確定したこと。

- 釣り場座標は **37.219960, 138.278409**（利用者が直接指定）
- **Yahoo!天気を反映させる。** 「個人的に参照する目的なのだからAPIが無くても
  毎回ページをチェックして形を変えて反映してください」
- **気象庁の予報と Yahoo は同じではない。** ← これは利用者の指摘が正しく、こちらの認識が誤りだった（§5参照）

---

## 3. 調べて確定した事実（実際にAPIを叩いて検証済み）

推測ではなく実測。同じ調査を繰り返さないこと。

| 項目 | 結論 |
|---|---|
| Open-Meteo Forecast | **16日先**まで。`models=` で4モデル同時取得可 |
| Open-Meteo Marine | **8日先まで。9日目以降は物理的に存在しない**（設計を大きく縛る） |
| `wind_speed_unit=ms` | **必須**。既定は km/h。付け忘れると数字が4倍近くなり判断を誤る |
| `wind_gusts_10m_jma_seamless` | **常に null**。モデル横断の中央値では欠損を必ず除外する |
| 波浪モデル | `gwam` は wave/wind_wave/swell 全部返す。`ecmwf_wam025` は swell/wind_wave が null |
| 東京湾内の波高 | 値は返るが約25km格子。**参考値**。湾内は風主体で判断する |
| 気象庁 潮位表 | 年間テキストが取得可。**固定長136文字**。CORS 不可なのでビルド時に同梱 |
| 潮汐地点記号 | 直江津=**T3** / 東京=**TK** / 横浜=**QS** |
| 気象庁 bosai JSON | CORS `*` で取れる。`winds` `waves`（沿岸の波）3日 + `reliabilities` A/B/C 7日 |
| Yahoo!天気 | **予報の公開APIは無い**（YOLPは前後1時間の降水強度のみ）。Webページは明後日から6日分 |
| Yahoo アプリのみの情報 | **信頼度A〜Cと7〜15日目はWebに無い**。非公開APIの中だけ（§6参照） |

### 潮位表の列定義（実データで検証済み）

```
 1 - 72   毎時潮位 3桁 × 24時間（cm・右詰め）
73 - 78   YY MM DD（2桁ずつ・空白パディング）
79 - 80   地点記号
81 -108   満潮 (時刻HHMM 4桁 + 潮位 3桁) × 4
109-136   干潮 同上 × 4        欠測は 9999 / 999
```

**時刻は「時2桁＋分2桁」で右詰め。** `" 4 8"` は 04:08 であって「48」ではない。
ここを整数として読むと満干潮時刻が全部壊れる。実際に踏んだ罠。

正解値として横浜 2026-01-01（満潮 04:08/163cm・14:08/169cm、干潮 08:58/123cm・21:32/1cm）を
`Build-Tide.ps1` の自己チェックと `tests.html` の両方に埋めてある。パーサを直したらここが守られているか見る。

---

## 4. 環境の罠（Windows PowerShell 5.1）

このマシンには **Node.js が無い**。ビルドツールも使わない。以下は全部実際に踏んだもの。

- **BOM無し UTF-8 の `.ps1` は ANSI(CP932) として読まれて文字化けする。**
  → `tools/*.ps1` は**意図的にASCIIのみ**。日本語が要るところは `[char]0x5E74` のように
  コードポイントから組み立てている。表示名は UTF-8 の `.js` 側に置く。**この方針を崩さないこと**（2回踏んだ）
- 三項演算子 `? :`、`??`、`?.` は **無い** → `if/else`
- **ハッシュテーブルのリテラル内に `if` 式や `-f` を書けない** → 先に変数へ入れる
- `param()` の既定値の中では **`$PSScriptRoot` が空**になることがある
  → 本体で `$MyInvocation.MyCommand.Path` にフォールバックして解決する
  （これで一度 `C:\data` へ書き出した。残骸の空ディレクトリがあるかもしれない）
- ネイティブexeへの `2>&1` は 5.1 では ErrorRecord に化けるので使わない

### ブラウザの罠（iOS Safari）— PCで確認しただけでは見つからない

利用者はスマホ（iPhone）で見る。**PCのEdge/Chromeだけで確認すると通り抜ける不具合がある。**

- **`border-collapse: collapse` の表のセルには `position: sticky` が効かない（WebKit）。**
  Chromium は効いてしまうので、PCでは正常に見える。`border-collapse: separate` + `border-spacing: 0` にする。
  罫線を各セルの `border-bottom` だけで引いていれば、separate にしても見た目は変わらない。
  **`table` の `border-collapse` を collapse に戻さないこと**（一度これで固定が効かなくなった）
- **CSS/JSがキャッシュされたままになる。** 直したはずの表示が反映されない原因になる。
  `pages.yml` が公開時に URL へコミットSHAを付けるようにしてある（リポジトリの中身は素のままなので
  `index.html` のダブルクリックは今も動く）。
  **`<link>` `<script>` を index.html に足したら、pages.yml の書き換え対象に入っているか確認すること**
- 見た目の修正をしたら、幅390px でも確認する（§11の手順）

### テストと目視確認の方法（Nodeが無いので）

Edge のヘッドレスを使う。

```powershell
# テスト（228件）
& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" `
  --headless=new --disable-gpu --user-data-dir="$env:TEMP\edge-ff" `
  --dump-dom "http://localhost:8765/tests.html" | Select-String 'RESULT:|FAIL'

# 見た目の確認
... --screenshot="out.png" --window-size=390,2400 "http://localhost:8765/"
```

スクロール後の状態など、静止画では確認できないものは
**index.html をコピーしてスクリプトを注入した一時HTMLを作り**、それを撮る（列固定の検証で実際に使った）。
一時ファイルは消すこと。

---

## 5. 判断を変えた出来事

### Yahoo!天気は気象庁の中継ではない（こちらの認識が誤りだった）

当初「Yahoo は日本気象協会経由で気象庁の予報を流しているだけだから、
気象庁を直接取れば足りる」と判断して Yahoo を実装しなかった。
利用者から「気象庁の予報とYahooは同じではない」と指摘され、実際に両方を取って比べたところ**違っていた**。

| 日付 | 気象庁 確度 | Yahoo 確度 | 気象庁 降水 | Yahoo 降水 |
|---|---|---|---|---|
| 7/30 | — | A | **80%** | **50%** |
| 7/31 | **C** | **A** | 70% | 60% |
| 8/1 | **B** | **C** | 30% | 40% |

日本気象協会の統計補正が入っている。現地の農業関係者が「Yahooが一番当たる」と言うのはこの部分。
**教訓: 上流が同じだろうという推測でデータソースを1本に減らさない。実測してから決める。**

結果として、割れている日は平均せず **食い違いをそのまま提示する**設計にした。
両者が割れている日＝予報が固まっていない日、という情報自体が価値になる。

### 出航地点の座標について

「出航地点の座標」を privacy 上の懸念として説明したが、調べ直すとそれは
こちらが推測で置いただけの未使用データだった。誤りとして撤回し、削除した。
**教訓: 自分が置いた仮のデータを、外部由来の事実のように語らない。**

---

## 6. 意図的にやらなかったこと

再提案されがちなので理由を残す。

- **Yahoo の非公開API（アプリ用）を叩く / スマホのプロファイルで通信を覗く**
  利用者から「スマホのプロファイルで情報をチェックできないのですか？」と聞かれた。
  技術的には可能だが、認証・署名を含む非公開エンドポイントの再現になるのでやらない。
  信頼度A〜Cが要る用途には**気象庁の公式確度（A/B/C・7日先）**を出している。
  この限界は README・コード・アプリ内マニュアルの3か所に明記済み
- **NOWPHAS の実測波浪** — 公開APIが安定していない
- **プッシュ通知** — 静的サイトでは無理。必要なら別途エージェント化
- **釣果ログ** — 別機能。スコープ外
- **ビルドツール / フレームワーク** — 素の `<script>` にしてあるので `index.html` を
  ダブルクリックするだけで動く（`file://` でも潮汐が読める）。港で圏外でも開ける価値のほうが大きい

---

## 7. 設計上こだわっている点（消さないこと）

- **波高が無い期間（9日目以降）を、あるように見せない。**
  ◎を付けず○止まりにし、`波高データなし` バッジを付け、候補の並びでも波浪データのある窓を先に出す。
  「制約が1つ少ないから上位に来ただけの日」を選ばせないため。
  さらに**単日の候補**を別枠で必ず出す（8日先の風だけの連続○より、波高まで揃った単日のほうが確実）
- **判定は窓内の最悪値**。平均だと一瞬の突風やうねりを見落とす
- **時間帯の境目は日の出・日の入から決める**。固定時刻だと季節でずれる。
  昼間は独立に定義せず「朝マヅメの終わり〜夕マヅメの始まり」とすることで隙間も重複も出ない。
  日照が短くて朝と夕が重なる日は南中±1時間にフォールバック
- **判定を色だけに依存させない**（◎○△× を必ず併記）。屋外の直射日光下で見るため
- **直江津は潮差が30cm前後しかない。** 東京湾（横浜で1.7m級）の「大潮＝釣れる」感覚が当てはまらないので、
  潮回りの名前だけでなく**実潮差(cm)を必ず併記**する
- **マニュアル（manual.js）は rating.js の定義から生成する。**
  手書きにするとルールを直したときにマニュアルだけ古くなる
- **`assets/rating.js` は純関数のみ**（DOM も fetch も触らない）。ここだけ読めば判定仕様が分かる状態を保つ
- **気象庁 bosai や Yahoo の取得に失敗しても本体の判定は止めない**。どちらも公式APIではない

---

## 8. Open-Meteo の 429 対策（触ると再発する）

6地点ぶんを一度に投げると **HTTP 429** が返り、釣り場が無言で欠ける。実際に発生した。

- 直江津だけ4モデル、東京湾5か所は**3モデル**（ECMWF / GFS / 気象庁）
- **ICONではなくGFSを残す**。16日目まで値があるのはGFSだけで、外すと最終日が「データなし」になる（一度やらかした）
- 同時実行は**2地点まで**、**直江津を先に**取る（制限に当たっても最重要地点は揃う）
- 429 は 2.5 秒待って1度だけ再試行

**モデルを減らす・並列数を上げる変更をするときは、必ず16日目が埋まっているか確認すること。**

---

## 9. 改修履歴

| # | 依頼 | やったこと |
|---|---|---|
| 1 | 初回構築 | 6地点の気象・波浪・潮汐、判定エンジン、連続凪ウィンドウ、モデル比較、GitHub Pages 公開 |
| 2 | GitHub と Vultr どちらが良いか | GitHub Pages を推奨（証明書・可用性・履歴の管理が不要）。Vultr へ置く手順も README に残した |
| 3 | 釣り場座標の指定 | 37.219960, 138.278409 に設定。未使用だった仮の出航地点データは削除 |
| 4 | 気象庁の公式予報を併記 | 沿岸の波・海上の風・確度A/B/C・解説文を取得。食い違い検出を追加 |
| 5 | 天気マークが小さい／降水確率も出したい | 16日ストリップのアイコンを拡大、降水確率%を併記（50%以上は赤）。上越・東京湾の全釣り場に適用 |
| 6 | 操作マニュアルが欲しい | **使い方タブ**を追加。表は判定エンジンの定義から生成 |
| 7 | 昼間・夕マヅメでも判定したい | 朝マヅメ/昼間/夕マヅメ/終日の選択式に。上越タブと東京湾タブで別々に保存 |
| 8 | Yahoo!天気を反映（再指示） | `Fetch-Yahoo.ps1` で週間予報を取得。モデル/気象庁/Yahoo の3者突き合わせに。1日4回の自動更新 |
| 9 | 突き合わせ表の日付が見えなくなる／単位表記 | 左端列を `position: sticky` で固定。「メートル」→「m」に統一 |
| 10 | 9がスマホで効いていない | `border-collapse` を separate に（WebKitの制約）。あわせて公開時のキャッシュ対策を追加（§4） |
| 11 | 確度の高いモデルの重みを上げたらどうか | 重み付けは**入れず**、代わりに A アンサンブル / B 代表値の選択 / C 答え合わせ を実装（§12） |

改修 9 の単位変更には落とし穴があった。**波高の解析はこの整形後の文字列を読んでいる**ため、
単純に置換すると「気象庁は沿岸の波を最大◯m」の警告が全部出なくなる。
解析側が「メートル」と `m` の両方を読めるようにし、`30mm`（降水量）を波高と誤読しないガードを入れてテストで固定した。
**表示用の整形関数を触るときは、その出力を誰かが解析していないか確認すること。**

---

## 9b. モデルの重み付けを入れなかった理由（改修11）

利用者から「確度の高いモデルの影響度（係数）を上げ、低いモデルを下げれば、より正確な予測になるのでは」と提案があった。
着眼点は正しいが、**適用する場所が違う**と判断して重み付けは入れず、代わりに3つを実装した。

**入れなかった理由（この順に重い）**

1. **モデルの食い違いは消すべきノイズではなく安全情報。**
   標準偏差を「一致度」として出しているのは平均を良くするためではなく、
   「この日の予報はまだ固まっていない」を伝えるため。重み付けするとばらつきが人為的に縮み、この信号が鈍る。
   2馬力3mの船で欲しいのは最も正確な中心値ではなく、**外したときにどれだけ危ないか**
2. **重みの根拠になるデータが無い。** 当時このアプリは予報を保存すらしていなかった。
   検証なしに係数を置けば、思い込みに数式の見た目を与えるだけになる
3. **16日目が壊れる。** その日まで値があるのは GFS だけなので、GFS を下げると
   一番先を見たい期間が一番不安定になる

**代わりに入れたもの**

| | 内容 | 効き方 |
|---|---|---|
| A | ECMWF アンサンブル51本 | 中心を精密にするのではなく、**裾**を見せる |
| B | 代表値の取り方を選択可（中央値／上側／最悪） | 係数を推定せずに安全側へ寄せられる |
| C | 予報と AMeDAS 実測の突き合わせ記録 | 将来 A/B の妥当性や重み付けを**根拠付きで**判断するための土台 |

**A が重み付けより効く理由**（実測、8日先の同一時刻）:
51本の中央値 1.3 m/s に対して最大 8.4 m/s。
決定論モデルを何本どう重み付けしても、この裾は絶対に見えない。

**重み付けは否定ではなく順番の問題。** C が十分たまり、リード別に明確な差が出たら、そのときは根拠を持って導入できる。
その判断ができるまで最低30件、季節が一巡するには1年かかる。

### C を触るときの注意

- **判定窓の定義が2か所にある。** `assets/rating.js` の `DEFAULTS.windowBeforeSunrise/AfterSunrise` と
  `tools/Record-Forecast.ps1` の `$WINDOW_BEFORE/$WINDOW_AFTER`。ずれると別の時間帯どうしを比べることになる。
  `tests.html` が `data/verify.js` に記録された窓と rating.js の定義の一致を検査しているので、
  片方だけ変えるとテストが落ちる（意図的にそうしてある）
- **アーカイブ由来（`src=archive`）をリード別集計に混ぜないこと。**
  過去日をあとから取り寄せた予報は発表直前の値に近く、実際よりずっと当たって見える。専用バケットに隔離してある
- **`data/verify-*.jsonl` は追記型の生ログ。消さないこと。** 集計 `data/verify.js` はいつでも作り直せる
- AMeDAS の点データは**約9日で消える**。取りこぼすとその日の実測は永久に埋まらない。
  日次ワークフローが止まっていないか、たまに確認すること
- 実測は AMeDAS 大潟（陸上の1点）。沖の海面ではないので、**モデル同士の相対比較にしか使えない**

### やってみて駄目だったこと

- Open-Meteo の `wind_speed_10m_previous_dayN` は**この地点では全て null**。
  リード別の過去予報を一括で埋められるかと思ったが使えなかった。
  代わりに historical-forecast-api を使ったが、これは短期予報なので別バケット扱いにしている

## 10. いま宙に浮いていること

利用者から依頼は出ていないが、判断が必要になったら思い出す事項。

- **Yahoo 由来の数値が public リポジトリに入っている。**
  個人参照が目的なので取得自体は利用者の判断で進めたが、公開リポジトリに置き続けるかは別の話。
  気になるなら (a) リポジトリを private にして Pages を有効化する、
  (b) 取得を Vultr の VPS 側に移して結果だけ配る、のどちらか。**まだ提案しただけで実施していない**
- **モデルの答え合わせの初回結果**。リード別の比較が始まるのは各リード30件たまってから。
  記録は毎日 JST 07:00 に走る。数週間後に設定タブを一度見ておくとよい。
- **2027年の潮位データ**。気象庁が翌年分を公表するのは年明け。
  `.github/workflows/tide.yml` が毎月6日に走って自動で拾うが、初回だけ動作を見ておくとよい
- Yahoo のページ構造が変わると `Fetch-Yahoo.ps1` が黙って空を返す可能性がある。
  日本語ラベルではなく**内容の形**（日付が3つ以上／`alt=` が3つ以上／`#ff3300`）で行を判定して耐性を上げてあるが、無敵ではない。
  `data/yahoo.js` の `fetchedAt` が古いままなら疑う

---

## 11. 変更するときの手順

1. `python -m http.server 8765 --directory .` で起動
2. `tests.html` を開く（またはヘッドレス）→ **228件すべてパス**を確認
3. `/`・`#shore`・`#settings`・`#manual` の4タブを開いてコンソールエラーが無いこと
4. スマホ幅（390px）で横スクロールが出ないこと
5. 判定ルールを変えたら **README と使い方タブの記述が追従しているか**確認
   （使い方タブは自動生成なので基本は追従する。README は手書き）
6. push すると GitHub Actions が自動デプロイ（15秒程度）。**本番でも1回開いて確認する**

なお `index.html` の `<!-- tide-data:start -->` ブロックは `Build-Tide.ps1` が書き換える。手で編集しない。

---

最終的な出船可否は現地の海況と気象庁・海上保安庁の警報で判断すること。このアプリは候補日の絞り込み用。
